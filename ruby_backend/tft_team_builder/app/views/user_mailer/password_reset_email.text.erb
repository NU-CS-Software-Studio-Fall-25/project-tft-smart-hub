TFT Smart Hub - Password Reset

Hi <%= @user.display_name.presence || @user.email %>,

Use this code to reset your password:

Code: <%= @reset_token %>

Or open the reset page directly:
<%= @reset_url %>

Both the link and code expire in 2 hours. If you didn’t request a new password, you can ignore this message.

— The TFT Smart Hub team
